services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  healer-base:
    build: 
      context: .
      dockerfile: Dockerfile.backend
      args:
        GA_ID: ${GA_ID:-GA_ID_PLACEHOLDER}
    image: healer:latest
    environment:
      - HEALER_REDIS_URL=redis://redis:6379/0

  backend:
    image: healer:latest
    command: uvicorn healer.web.app:app --host 0.0.0.0 --port 8000 --reload --concurrency 8 --loglevel info
    environment:
      - HEALER_REDIS_URL=redis://redis:6379/0
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - redis
      - healer-base

  worker:
    image: healer:latest
    command: celery -A healer.web.celery_worker.celery_app worker --loglevel=info
    environment:
      - HEALER_REDIS_URL=redis://redis:6379/0
    volumes:
      - .:/app
    depends_on:
      - redis
      - healer-base

  frontend:
    build:
      context: ./web_client
      dockerfile: Dockerfile.dev
    volumes:
      - ./web_client:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    environment:
      - VITE_API_TARGET=http://backend:8000
    depends_on:
      - backend
